# 复制为 .env 后填入真实值：cp .env.example .env
#
# ⚠️ 只有 .env.example 会进版本库，.env 已在 .gitignore 中。
#    真实 key 绝不能写进这个文件。

# 硅基流动 API Key，用于调用 Embedding 模型
# 获取地址：https://cloud.siliconflow.cn/account/ak
#
# ⚠️ 把 CONTEXTDOCK_FAKE_EMBEDDER 设为 true 时，这一项**不再必填**。
SILICONFLOW_API_KEY=sk-replace-me

# PostgreSQL 连接串（M5 之后才需要）
CONTEXTDOCK_DATABASE_URL=postgres://postgres:postgres@localhost:5432/contextdock?sslmode=disable

# ---- 只想先跑跑看？打开下面两行，上面两项都可以不填 ----
#
# 假嵌入：不联网、不调用 API，向量由 token 哈希现造。
# ⚠️ 它**没有语义理解**——问「怎么让程序跑得快」，它找不到写着
#    「性能优化」的段落，而那正是混合检索存在的理由。
#    所以它只验证链路通不通，不反映真实检索质量。
#    要判断质量请用 `go run ./cmd/eval`，要真实效果请配真 Key。
#
# 内存存储：数据不落盘，重启即丢。适合试用，不适合日常使用。
#
# CONTEXTDOCK_FAKE_EMBEDDER=true
# CONTEXTDOCK_USE_MEMORY_STORE=true
